# git push mobile

Mobile-friendly web app to commit & push to GitHub repos — single-file edits,
whole-folder pushes (one commit per file), and commit backdating via the Git Data API.

## Run locally
npm install
npm run dev

## Deploy
Push to GitHub and import the repo on vercel.com — Vite is auto-detected.
